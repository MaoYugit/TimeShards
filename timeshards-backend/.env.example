# 复制为 .env 后修改

PORT=3001

# MongoDB 连接串（本地示例）
MONGODB_URI=mongodb://127.0.0.1:27017/timeshards

# 管理接口：写入/删除文章、清空等需要携带此头：X-API-Key: <值>
# 开发可留空则跳过校验（不推荐生产）
ADMIN_API_KEY=

# 允许的 Web 前端来源（逗号分隔），用于 CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
